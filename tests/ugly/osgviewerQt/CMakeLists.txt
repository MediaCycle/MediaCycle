SET(TARGET_NAME "osgviewerQt") # from the OSG examples
IF(USE_OSG AND USE_QT4)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_EXECUTABLE(${TARGET_NAME} ${SRC} ${HDR})

        TARGET_LINK_LIBRARIES(${TARGET_NAME} ${OPENSCENEGRAPH_LIBRARIES} ${QT_LIBRARIES} ${OPENGL_LIBRARIES})

	SET_TARGET_PROPERTIES("${TARGET_NAME}" PROPERTIES FOLDER "${FOLDERNAME}")
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()
