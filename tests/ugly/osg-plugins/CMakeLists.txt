IF(OSG_FOUND)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_EXECUTABLE(osg-plugins-test ${SRC} ${HDR})
	IF(MINGW AND CMAKE_TOOLCHAIN_FILE)
		TARGET_LINK_LIBRARIES(osg-plugins-test ${OSGPLUGINS_STATIC_LDFLAGS})#${OSGPLUGINS_LDFLAGS})
	ENDIF()

        TARGET_LINK_LIBRARIES(osg-plugins-test ${OPENSCENEGRAPH_LIBRARIES} ${OPENGL_LIBRARIES})

	SET_TARGET_PROPERTIES("osg-plugins-test" PROPERTIES FOLDER "${FOLDERNAME}")
	MESSAGE("[X] osg-plugins-test")
ELSE()
	MESSAGE("[ ] osg-plugins-test")
ENDIF()
