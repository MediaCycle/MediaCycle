SET(TARGET_NAME "octave_embed")
IF(OCTAVE_FOUND)
	FILE(GLOB SRC *.cc *.c)
	FILE(GLOB HDR *.h)

	ADD_LIBRARY(${TARGET_NAME} ${SRC} ${HDR})

	TARGET_LINK_LIBRARIES(${TARGET_NAME} ${OCTAVE_LIBRARY})

	#INSTALL(TARGETS ${TARGET_NAME} LIBRARY DESTINATION lib)
	#INSTALL(FILES ${HDR} DESTINATION include/${TARGET_NAME})
	SET_TARGET_PROPERTIES("${TARGET_NAME}" PROPERTIES FOLDER "${FOLDERNAME}")
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()
