# http://code.google.com/p/cvgabor/
SET(TARGET_NAME "cvgabor")
IF(OpenCV_FOUND AND FFTW3_FOUND)
	FILE(GLOB SRC *.cpp)
	FILE(GLOB HDR *.h)

	ADD_LIBRARY(${TARGET_NAME} ${SRC} ${HDR})
	TARGET_LINK_LIBRARIES(${TARGET_NAME} ${OpenCV_LIBRARIES} ${FFTW3_LIBRARY})

	#INSTALL(TARGETS ${TARGET_NAME} LIBRARY DESTINATION lib)
	#INSTALL(FILES ${HDR} DESTINATION include/${TARGET_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()
