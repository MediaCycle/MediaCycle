SET(TARGET_NAME "KinectCycle")
SET(ICON_NAME "${TARGET_NAME}")
IF( USE_QT4 AND USE_OSC AND USE_OPENNI AND FFTW3_FOUND AND USE_BREAKPAD)

        SET(DESCRIPTION "KinectCycle allows to control MediaCycle applications with multiple people though OSC.")

        INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddQtApp.cmake)
	ADD_QT_EXECUTABLE(${TARGET_NAME})

	TARGET_LINK_LIBRARIES(${TARGET_NAME} ${LIBLO_LIBRARIES} ${OPENNI_LIBRARIES} ${FFTW3_LIBRARY})

	MESSAGE("[X] ${TARGET_NAME}")

	# Packaging
	SET(PROGNAME "${TARGET_NAME}")
	IF(NOT USE_DEBUG)
		INCLUDE (${CMAKE_SOURCE_DIR}/cmake/CreatePackage.cmake)
	ENDIF()
	SET_TARGET_PROPERTIES(${TARGET_NAME} PROPERTIES FOLDER "${FOLDERNAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()
