SET(TARGET_NAME "navimedbiotreedatascript")
IF(SUPPORT_TEXT AND BUILD_CLIS)
	FILE(GLOB SRC *.cpp)
	FILE(GLOB HDR *.h)

	IF ( APPLE )
		SET(APP_TYPE MACOSX_BUNDLE)
	ENDIF ( APPLE )

	ADD_EXECUTABLE(navimedbiotreedatascript ${SRC} ${HDR})
	IF(TINYXML_FOUND)
		TARGET_LINK_LIBRARIES(navimedbiotreedatascript ${TINYXML_LIBRARY})
	ELSE()
		ADD_DEPENDENCIES(navimedbiotreedatascript tinyxml)
		TARGET_LINK_LIBRARIES(navimedbiotreedatascript tinyxml)
	ENDIF()


	#IF ( APPLE )
	#	set_target_properties(text-test PROPERTIES MACOSX_BUNDLE_INFO_PLIST ${CMAKE_SOURCE_DIR}/gui/osg-qt/Info.plist)
	#	set_source_files_properties(PROPERTIES MACOSX_PACKAGE_LOCATION Resources)
	#ENDIF ( APPLE )

	IF ( APPLE )
		set(CMAKE_EXE_LINKER_FLAGS "-framework CoreVideo -framework AppKit -framework Foundation")
	ENDIF ( APPLE )
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()
