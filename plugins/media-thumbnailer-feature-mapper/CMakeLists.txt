SET(PLUGIN_NAME "media-thumbnailer-feature-mapper")
list(APPEND 3RDPARTY_NAMES "simple-svg")
list(APPEND 3RDPARTY_LICENSES "BSD")
list(APPEND 3RDPARTY_URLS "http://code.google.com/p/simple-svg/")
#IF()
	INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/3rdparty/simple-svg)
        INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
        TARGET_LINK_LIBRARIES(${TARGET_NAME} simple-svg)
        INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/libs/gui-osg)
        TARGET_LINK_LIBRARIES(${TARGET_NAME} mediacycle-osg)
	MESSAGE("[X] ${TARGET_NAME}")
#ELSE()
#	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
#ENDIF()
