SET(MEDIA_TYPE "audio")
SET(PLUGIN_NAME "audio-engine-stk-sounddesign")
IF(NOT STK_FOUND)
SET(MC_3RDPARTIES "RtAudio;RtMidi;stk")
ENDIF()
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core;audio-engine-stk")
SET(WITH_QT ON)
list(APPEND 3RDPARTY_NAMES "Synthesis ToolKit in C++ (STK)")
list(APPEND 3RDPARTY_LICENSES "free")
list(APPEND 3RDPARTY_URLS "https://ccrma.stanford.edu/software/stk/")
list(APPEND 3RDPARTY_NAMES "RtAudio")
list(APPEND 3RDPARTY_LICENSES "free")
list(APPEND 3RDPARTY_URLS "http://www.music.mcgill.ca/~gary/rtaudio/")
list(APPEND 3RDPARTY_NAMES "RtMidi")
list(APPEND 3RDPARTY_LICENSES "free")
list(APPEND 3RDPARTY_URLS "http://www.music.mcgill.ca/~gary/rtmidi/")
IF(SUPPORT_AUDIO AND (NOT APPLE OR (COREAUDIO_FOUND AND COREFOUNDATION_FOUND)) AND (USE_QT4 OR USE_QT5))
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
    INCLUDE_DIRECTORIES(${STK_INCLUDE_DIRS})
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
