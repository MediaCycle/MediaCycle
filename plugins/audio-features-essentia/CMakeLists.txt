SET(MEDIA_TYPE "audio")
SET(PLUGIN_NAME "audio-features-essentia")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core")
list(APPEND 3RDPARTY_NAMES "Essentia")
list(APPEND 3RDPARTY_LICENSES "AGPL")
list(APPEND 3RDPARTY_URLS "http://http://essentia.upf.edu")
IF(SUPPORT_AUDIO AND USE_ESSENTIA AND ESSENTIA_FOUND AND GAIA_FOUND)

        INCLUDE_DIRECTORIES(${ESSENTIA_INCLUDE_DIR})

	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})

	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${ESSENTIA_LIBRARIES} ${GAIA_LIBRARIES})

	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
