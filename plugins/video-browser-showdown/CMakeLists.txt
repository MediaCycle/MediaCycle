SET(MEDIA_TYPE "video")
SET(PLUGIN_NAME "video-browser-showdown")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core")
SET(WITH_QT ON)
SET(MC_3RDPARTIES "hidapi")
list(APPEND 3RDPARTY_NAMES "signal11's hidapi")
list(APPEND 3RDPARTY_LICENSES "BSD")
list(APPEND 3RDPARTY_URLS "https://github.com/signal11/hidapi/")
list(APPEND 3RDPARTY_NAMES "libcurl")
list(APPEND 3RDPARTY_LICENSES "MIT")
list(APPEND 3RDPARTY_URLS "http://curl.haxx.se/")
IF(SUPPORT_VIDEO AND (USE_QT4 OR USE_QT5) AND CURL_FOUND AND APPLE)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
        TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${CURL_LIBRARIES})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
