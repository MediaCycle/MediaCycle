SET(PLUGIN_NAME "visualisation-s-hull")
SET(MC_3RDPARTIES "s-hull")
list(APPEND 3RDPARTY_NAMES "s-hull")
list(APPEND 3RDPARTY_LICENSES "Beerware")
list(APPEND 3RDPARTY_URLS "http://www.s-hull.org")
SET(WITH_QT4 ON)
IF(USE_QT4)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
