SET(PLUGIN_NAME "audio-vamp")
SET(MC_MEDIA_LIBS "audio-core;audio-vamp")
list(APPEND 3RDPARTY_NAMES "Vamp plugin SDK")
list(APPEND 3RDPARTY_LICENSES "BSD")
list(APPEND 3RDPARTY_URLS "http://vamp-plugins.org")
IF(SUPPORT_AUDIO AND USE_VAMP)
        INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/3rdparty/vamp-plugin-sdk)
        INCLUDE (${CMAKE_SOURCE_DIR}/CMakeModules/AddMediaCyclePlugin.cmake)
        ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
