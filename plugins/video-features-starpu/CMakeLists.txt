SET(MEDIA_TYPE "video")
SET(PLUGIN_NAME "video-features-starpu")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core")
list(APPEND 3RDPARTY_NAMES "OpenCV")
list(APPEND 3RDPARTY_LICENSES "BSD")
list(APPEND 3RDPARTY_URLS "http://opencv.willowgarage.com")
list(APPEND 3RDPARTY_NAMES "StarPU")
list(APPEND 3RDPARTY_LICENSES "LGPL")
list(APPEND 3RDPARTY_URLS "http://runtime.bordeaux.inria.fr/StarPU/")
IF(SUPPORT_VIDEO AND OpenCV_FOUND AND STARPU_USE_CPU)

	INCLUDE (${CMAKE_SOURCE_DIR}/CMakeModules/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	TARGET_LINK_LIBRARIES(${TARGET_NAME} ${OpenCV_LIBRARIES})

	IF (STARPU_USE_CPU)
		TARGET_LINK_LIBRARIES(${TARGET_NAME} ${STARPU_LIBRARY})
	ENDIF()

	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
