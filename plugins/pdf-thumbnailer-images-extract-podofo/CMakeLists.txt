SET(MEDIA_TYPE "pdf")
SET(PLUGIN_NAME "pdf-thumbnailer-images-extract-podofo")
SET(MC_3RDPARTIES "PoDoFo-tools")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core")
list(APPEND 3RDPARTY_NAMES "PoDoFo")
list(APPEND 3RDPARTY_LICENSES "LGPL")
list(APPEND 3RDPARTY_URLS "http://podofo.sourceforge.net")
list(APPEND 3RDPARTY_NAMES "PoDoFo tools")
list(APPEND 3RDPARTY_LICENSES "GPL")
list(APPEND 3RDPARTY_URLS "http://podofo.sourceforge.net")
IF(SUPPORT_PDF AND PODOFO_FOUND)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
        TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${PODOFO_LIBRARIES})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
