SET(MEDIA_TYPE "audio")
SET(PLUGIN_NAME "audio-engine")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core;${MEDIA_TYPE}-engine")
IF(USE_OPENAL)
    list(APPEND 3RDPARTY_NAMES "OpenAL-soft")
    list(APPEND 3RDPARTY_LICENSES "LGPL")
    list(APPEND 3RDPARTY_URLS "http://kcat.strangesoft.net/openal.html")
ENDIF()
IF(USE_PORTAUDIO)
    list(APPEND 3RDPARTY_NAMES "PortAudio")
    list(APPEND 3RDPARTY_LICENSES "MIT")
    list(APPEND 3RDPARTY_URLS "http://www.portaudio.com")
ENDIF()
list(APPEND 3RDPARTY_NAMES "The Speech Signal Processing Toolkit (SPTK)")
list(APPEND 3RDPARTY_LICENSES "BSD")
list(APPEND 3RDPARTY_URLS "http://sp-tk.sourceforge.net")
IF(SUPPORT_AUDIO AND (USE_OPENAL OR USE_PORTAUDIO))
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	SET_TARGET_PROPERTIES(${PLUGIN_PREFIX}${PLUGIN_NAME} PROPERTIES FOLDER "${FOLDERNAME}")
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
