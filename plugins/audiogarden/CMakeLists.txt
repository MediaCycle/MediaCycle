SET(MEDIA_TYPE "audio")
SET(PLUGIN_NAME "audiogarden")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core;${MEDIA_TYPE}-features;${MEDIA_TYPE}-engine;${MEDIA_TYPE}-osg;audiogarden-synthesis;audiogarden-qt")
SET(WITH_QT4 ON)
list(APPEND 3RDPARTY_NAMES "libsndfile")
list(APPEND 3RDPARTY_LICENSES "LGPL or GPL")
list(APPEND 3RDPARTY_URLS "http://www.mega-nerd.com/libsndfile")
list(APPEND 3RDPARTY_NAMES "libsamplerate")
list(APPEND 3RDPARTY_LICENSES "GPL")
list(APPEND 3RDPARTY_URLS "http://www.mega-nerd.com/SRC/")
IF(SUPPORT_AUDIO AND USE_AUDIOGARDEN AND USE_AUDIOFEAT AND SNDFILE_FOUND AND BUILD_APPS AND USE_OSG)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
        TARGET_LINK_LIBRARIES(${TARGET_NAME} ${SNDFILE_LIBRARIES})
        INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/libs/gui-osg-qt)
        TARGET_LINK_LIBRARIES(${TARGET_NAME} mediacycle-osg-qt)
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
