SET(PLUGIN_NAME "visualisation-dancers")
IF(SUPPORT_VIDEO)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR/libs/visualisation})
	TARGET_LINK_LIBRARIES(${TARGET_NAME} mediacycle-visualisation)
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()