SET(PLUGIN_NAME "io-novint-falcon")
SET(MC_3RDPARTIES "hidapi")
SET(WITH_QT4 ON)
IF(USE_OSG AND USE_QT4 AND USE_FALCON AND LIBNIFALCON_FOUND AND LIBUSB_FOUND)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
        TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${LIBNIFALCON_LIBRARIES} ${LIBUSB_LIBRARY})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
