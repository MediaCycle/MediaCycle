SET(PLUGIN_NAME "visualisation-divvy")
SET(MC_3RDPARTIES "Divvy")
list(APPEND 3RDPARTY_NAMES "Divvy")
list(APPEND 3RDPARTY_LICENSES "New BSD License")
list(APPEND 3RDPARTY_URLS "https://github.com/jmlewis/divvy/")
IF(LIBDISPATCH_FOUND AND APPLE)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
