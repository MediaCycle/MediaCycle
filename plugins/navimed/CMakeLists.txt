SET(PLUGIN_NAME "navimed")
SET(TARGET_NAME "${PLUGIN_PREFIX}${PLUGIN_NAME}")
IF(SUPPORT_NAVIMED AND SUPPORT_AUDIO AND SUPPORT_TEXT AND SUPPORT_IMAGE)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${TARGET_NAME} SHARED ${SRC} ${HDR})
	ADD_DEPENDENCIES(${TARGET_NAME} mediacycle textTool)
	SET_TARGET_PROPERTIES(${TARGET_NAME} PROPERTIES PREFIX ${PLUGIN_PREFIX})
	SET_TARGET_PROPERTIES(${TARGET_NAME} PROPERTIES OUTPUT_NAME ${PLUGIN_NAME})

	TARGET_LINK_LIBRARIES(${TARGET_NAME} mediacycle)
	TARGET_LINK_LIBRARIES(${TARGET_NAME} textTool)
	TARGET_LINK_LIBRARIES(${TARGET_NAME} navimed)
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()
