SET(PLUGIN_NAME "visualisation-advanced-mlpack")
SET(MC_LIBS "armadillo-visualisation;visualisation")
IF(USE_DEBUG)
IF(MLPACK_FOUND)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
       TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${MLPACK_LIBRARIES} ${LIBXML2_LIBRARIES} ${ARMADILLO_LIBRARIES})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
