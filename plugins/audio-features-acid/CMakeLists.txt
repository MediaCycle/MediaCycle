SET(MEDIA_TYPE "audio")
SET(PLUGIN_NAME "audio-features-acid")
SET(MC_3RDPARTIES "cwaves")
SET(MC_MEDIA_LIBS "${MEDIA_TYPE}-core")
list(APPEND 3RDPARTY_NAMES "CwavesAcid")
list(APPEND 3RDPARTY_LICENSES "?")
list(APPEND 3RDPARTY_URLS "?")
IF(SUPPORT_AUDIO AND USE_OSG)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
