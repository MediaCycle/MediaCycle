SET(PLUGIN_NAME "visualisation-lineartimenerv")
SET(MC_3RDPARTIES "lineartimenerv")
list(APPEND 3RDPARTY_NAMES "LinearTimeNeRV")
list(APPEND 3RDPARTY_LICENSES "LPGL")
list(APPEND 3RDPARTY_URLS "http://research.ics.aalto.fi/mi/software/")
list(APPEND 3RDPARTY_NAMES "Newmat")
list(APPEND 3RDPARTY_LICENSES "?")
list(APPEND 3RDPARTY_URLS "http://www.robertnz.net/nm_intro.htm")
IF(ARMADILLO_FOUND AND NEWMAT_FOUND)
	INCLUDE (${CMAKE_SOURCE_DIR}/cmake/AddMediaCyclePlugin.cmake)
	ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
