SET(PLUGIN_NAME "eyesweb")
IF ( ARMADILLO_FOUND AND USE_EYESWEB)
        ADD_MC_PLUGIN_LIBRARY(${PLUGIN_NAME})
        TARGET_LINK_LIBRARIES(${TARGET_NAME} mediacycle armadillo-utils ${ARMADILLO_LIBRARIES})
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${PLUGIN_PREFIX}${PLUGIN_NAME}")
ENDIF()
