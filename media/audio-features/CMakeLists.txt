SET(MEDIA_TYPE "audio")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-features")
IF(SUPPORT_AUDIO AND SNDFILE_FOUND AND SAMPLERATE_FOUND AND USE_AUDIOFEAT)
	INCLUDE_DIRECTORIES(../${MEDIA_TYPE}-core)

	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	ADD_DEPENDENCIES(${LIBRARY_NAME} mediacycle-${MEDIA_TYPE}-core)
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${SNDFILE_LIBRARIES} ${SAMPLERATE_LIBRARY} mediacycle-${MEDIA_TYPE}-core)

	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib)
	#INSTALL(FILES ${HDR} DESTINATION include/${LIBRARY_NAME})
	SET_TARGET_PROPERTIES(${LIBRARY_NAME} PROPERTIES FOLDER "${FOLDERNAME}")
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()
