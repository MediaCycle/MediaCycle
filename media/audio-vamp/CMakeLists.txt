SET(MEDIA_TYPE "audio")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-vamp")
IF(SUPPORT_AUDIO AND USE_VAMP AND SNDFILE_FOUND)
        INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/3rdparty/vamp-plugin-sdk)
	INCLUDE_DIRECTORIES(../${MEDIA_TYPE}-core)

	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	ADD_DEPENDENCIES(${LIBRARY_NAME} vamp-simple-hostsdk mediacycle-${MEDIA_TYPE}-core)
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} vamp-hostsdk vamp-sdk mediacycle mediacycle-${MEDIA_TYPE}-core ${SNDFILE_LIBRARIES})
	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib/${LIBRARY_NAME})
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()


