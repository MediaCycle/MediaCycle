SET(MEDIA_TYPE "video")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-osg")
IF(USE_OSG AND SUPPORT_VIDEO)
	INCLUDE_DIRECTORIES(../${MEDIA_TYPE}-core ../image-osg)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	ADD_DEPENDENCIES(${LIBRARY_NAME} mediacycle-osg mediacycle-${MEDIA_TYPE}-core mediacycle-image-osg)
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} mediacycle-osg mediacycle-${MEDIA_TYPE}-core mediacycle-image-osg ${OpenCV_LIBRARIES})
	IF(FFMPEG_FOUND AND USE_FFMPEG)
		TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${FFMPEG_LIBRARIES})
	ENDIF()
	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib/${LIBRARY_NAME})
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()