SET(MEDIA_TYPE "video")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-osg")
SET(OSG_PLUGINS "osgdb_ffmpeg")
IF(USE_OSG AND SUPPORT_VIDEO)
        INCLUDE_DIRECTORIES(../${MEDIA_TYPE}-core)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
        ADD_DEPENDENCIES(${LIBRARY_NAME} mediacycle-osg mediacycle-${MEDIA_TYPE}-core)
        TARGET_LINK_LIBRARIES(${LIBRARY_NAME} mediacycle-osg mediacycle-${MEDIA_TYPE}-core ${OpenCV_LIBRARIES})
	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib/${LIBRARY_NAME})
	SET_TARGET_PROPERTIES(${LIBRARY_NAME} PROPERTIES FOLDER "${FOLDERNAME}")

	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()
