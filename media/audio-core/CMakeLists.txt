SET(MEDIA_TYPE "audio")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-core")
IF(SUPPORT_AUDIO AND SNDFILE_FOUND)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	ADD_DEPENDENCIES(${LIBRARY_NAME} mediacycle)
        TARGET_LINK_LIBRARIES(${LIBRARY_NAME} mediacycle ${SNDFILE_LIBRARY})
	IF ( MINGW )
		TARGET_LINK_LIBRARIES(mediacycle FLAC vorbisenc vorbis m ogg)
	ENDIF()
	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib/${LIBRARY_NAME})
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()
