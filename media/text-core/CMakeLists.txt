SET(MEDIA_TYPE "text")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-core")
IF(SUPPORT_TEXT AND CLUCENE_FOUND)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	IF(TINYXML_FOUND)
		TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${TINYXML_LIBRARY})
	ELSE()
		ADD_DEPENDENCIES(${LIBRARY_NAME} tinyxml)
		TARGET_LINK_LIBRARIES(${LIBRARY_NAME} tinyxml)
	ENDIF()
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} mediacycle ${CLUCENE_LIBRARIES} ${Boost_LOCALE_LIBRARY} ${Boost_SYSTEM_LIBRARY} ${Boost_FILESYSTEM_LIBRARY})
	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib/${LIBRARY_NAME})
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()
