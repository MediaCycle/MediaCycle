SET(MEDIA_TYPE "audio")
SET(LIBRARY_NAME "mediacycle-${MEDIA_TYPE}-features-laughter")
IF(SUPPORT_AUDIO AND SNDFILE_FOUND AND SAMPLERATE_FOUND AND USE_LAUGHFEAT)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	ADD_DEPENDENCIES(${LIBRARY_NAME} mediacycle mediacycle-audio)
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} ${SNDFILE_LIBRARY} ${SAMPLERATE_LIBRARY} fftooura mediacycle-audio armadillo-utils ${ARMADILLO_LIBRARIES})

	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib)
	#INSTALL(FILES ${HDR} DESTINATION include/${LIBRARY_NAME})
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()
