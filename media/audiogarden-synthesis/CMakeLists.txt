SET(MEDIA_TYPE "audio")
SET(LIBRARY_NAME "mediacycle-audiogarden-synthesis")
IF(SUPPORT_AUDIO AND USE_AUDIOGARDEN AND USE_AUDIOFEAT)
	INCLUDE_DIRECTORIES(../${MEDIA_TYPE}-core ../${MEDIA_TYPE}-features)
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)
	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	ADD_DEPENDENCIES(${LIBRARY_NAME} mediacycle-${MEDIA_TYPE}-core mediacycle-${MEDIA_TYPE}-features)
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} mediacycle-${MEDIA_TYPE}-core mediacycle-${MEDIA_TYPE}-features)
	#INSTALL(TARGETS ${LIBRARY_NAME} LIBRARY DESTINATION lib/${LIBRARY_NAME})
	MESSAGE("[X] ${LIBRARY_NAME}")
ELSE()
	MESSAGE("[ ] ${LIBRARY_NAME}")
ENDIF()