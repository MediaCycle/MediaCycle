IF ( USE_OSG )
	FILE(GLOB SRC *.cpp)
	FILE(GLOB HDR *.h)

	ADD_LIBRARY(mediacycle-osg ${SRC} ${HDR})
	ADD_DEPENDENCIES(mediacycle-osg mediacycle)
	IF(SUPPORT_AUDIO)
		ADD_DEPENDENCIES(mediacycle-osg audioengine)
	ENDIF()

	TARGET_LINK_LIBRARIES(mediacycle-osg mediacycle)
	TARGET_LINK_LIBRARIES(mediacycle-osg ${OPENSCENEGRAPH_LIBRARIES})
	IF ( MINGW )#OR APPLE_IOS_DEV OR APPLE_IOS_SIM )
		TARGET_LINK_LIBRARIES(mediacycle-osg ${OSGPLUGINS_STATIC_LDFLAGS})#${OSGPLUGINS_LDFLAGS})
	ENDIF ( MINGW )#OR APPLE_IOS_DEV OR APPLE_IOS_SIM )
	TARGET_LINK_LIBRARIES(mediacycle-osg ${OPENGL_LIBRARIES})
	TARGET_LINK_LIBRARIES(mediacycle-osg ${Boost_SYSTEM_LIBRARY})
	IF(SUPPORT_AUDIO)
		TARGET_LINK_LIBRARIES(mediacycle-osg audioengine)
	ENDIF()
	IF(SUPPORT_IMAGE OR SUPPORT_VIDEO)
		TARGET_LINK_LIBRARIES(mediacycle-osg ${OpenCV_LIBRARIES})
	ENDIF()

	IF(SUPPORT_VIDE0 AND FFMPEG_FOUND AND WITH_FFMPEG)
		TARGET_LINK_LIBRARIES(mediacycle-osg ${FFMPEG_LIBRARIES})
	ENDIF()

	MESSAGE("[X] mediacycle-osg")
ELSE()
	MESSAGE("[ ] mediacycle-osg")
ENDIF()
