SET(LIBRARY_NAME "mediacycle-visualisation")
#IF()
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(${LIBRARY_NAME} ${SRC} ${HDR})
	TARGET_LINK_LIBRARIES(${LIBRARY_NAME} mediacycle armadillo-utils)
	SET_TARGET_PROPERTIES(${LIBRARY_NAME} PROPERTIES FOLDER "${FOLDERNAME}")

	MESSAGE("[X] ${LIBRARY_NAME}")
#ELSE()
#	MESSAGE("[ ] ${LIBRARY_NAME}")
#ENDIF()
