SET(TARGET_NAME "mlpack-utils")
IF ( MLPACK_FOUND )
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)
	
	ADD_LIBRARY(${TARGET_NAME} ${SRC} ${HDR})
	
	TARGET_LINK_LIBRARIES(${TARGET_NAME} ${MLPACK_LIBRARY})

	SET_TARGET_PROPERTIES("${TARGET_NAME}" PROPERTIES FOLDER "libs")
    
	MESSAGE("${FOLDERNAME}")
	MESSAGE("[X] ${TARGET_NAME}")
ELSE()
	MESSAGE("[ ] ${TARGET_NAME}")
ENDIF()

