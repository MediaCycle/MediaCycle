IF(CLUCENE_FOUND)
#	MESSAGE("Building textTool ")
	FILE(GLOB SRC *.cpp *.c)
	FILE(GLOB HDR *.hpp *.h)

	ADD_LIBRARY(textTool STATIC ${SRC} ${HDR})
	IF(TINYXML_FOUND)
		TARGET_LINK_LIBRARIES(textTool ${TINYXML_LIBRARY})
	ELSE()
		ADD_DEPENDENCIES(textTool tinyxml)
		TARGET_LINK_LIBRARIES(textTool tinyxml)
	ENDIF()
	#ADD_DEPENDENCIES(textTool archipel)
	#IF (SUPPORT_NAVIMED )
	#	ADD_DEPENDENCIES(textTool navimed)
	#ENDIF()

	TARGET_LINK_LIBRARIES(textTool ${CLUCENE_LIBRARIES} )
	
	TARGET_LINK_LIBRARIES(textTool ${Boost_LOCALE_LIBRARY})
#	IF (SUPPORT_ARCHIPEL )
#	TARGET_LINK_LIBRARIES(textTool archipel)
#	ENDIF()
#	IF (SUPPORT_NAVIMED )
#	TARGET_LINK_LIBRARIES(textTool navimed)
#	ENDIF()
	MESSAGE("[X] TextTool")
ELSE()
	MESSAGE("[ ] TextTool (clucene not found)")
ENDIF()
